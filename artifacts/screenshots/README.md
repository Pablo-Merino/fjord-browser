# Screenshots

Save routine headless and laptop captures under `runs/`. Git ignores that
directory.

Move only reviewed milestone or regression images into `baselines/` when they
are useful enough to preserve in version control. Captures must not contain
credentials, private page content, notifications, or unrelated desktop areas.
